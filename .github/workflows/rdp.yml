name: مكتب ندر الرقمي

on: workflow_dispatch

jobs:
  تشغيل_الجهاز:
    runs-on: windows-latest

    steps:
    - name: 🛠 تحميل وتثبيت Getscreen
      shell: powershell
      run: |
        $url = "https://app.getscreen.me/downloads/windows"
        $output = "agent.exe"
        Invoke-WebRequest -Uri $url -OutFile $output
        Start-Process -FilePath $output -ArgumentList "/email naderhussein59@gmail.com" -Wait

    - name: ✅ تأكيد الربط
      run: echo "🎭 تم تثبيت Agent وربط الجهاز بحساب ندر. المكتب الرقمي جاهز للتحكم."
